# Media Placeholders

This folder contains placeholder images. Replace with real photos for production:

- `brand/hero-logo.jpeg` - Main hero/logo image
- `gallery/*.jpeg` - Gallery and city page images

Run `node scripts/create-placeholders.js` to regenerate placeholders if needed.
