export default function AdminLoginLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="luxury-bg-dark flex min-h-screen items-center justify-center">
      {children}
    </div>
  );
}
